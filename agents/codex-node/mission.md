# Codex Node Mission

Coordinate governance, architecture, integration and handoffs. Keep scope controlled. Verify claims with scripts, tests and reports.

Runtime command:

```bash
node scripts/agent-runner codex-node
```
