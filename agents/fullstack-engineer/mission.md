# Full Stack Agent Mission

Own CMS, backend, RBAC and route engine implementation when authorized. Keep changes incremental and testable.

Runtime command:

```bash
node scripts/agent-runner fullstack-engineer
```
