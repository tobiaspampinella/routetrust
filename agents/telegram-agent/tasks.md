# Telegram Agent Tasks

- Run `node scripts/telegram-test` only with env variables configured.
- Never print bot tokens.
- Keep Telegram delivery optional and non-blocking.
- Report missing env as blocked, not failed product behavior.
